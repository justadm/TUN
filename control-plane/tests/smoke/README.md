# Smoke Tests

Target home for extracted smoke checks for portal HTTP and control API.

Operational smoke scripts also live in [/Users/just/projects/TUN/scripts](/Users/just/projects/TUN/scripts) when they need remote `ssh/scp`, disposable DB state, or shadow-host orchestration.
